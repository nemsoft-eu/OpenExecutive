{
  "section_id": "org",
  "title": "Org Structure (Departments & People)",
  "markdown": "The org model has two persistent registries backed by SQLite (both share `episodic_memory.db`): **departments** and **people**. Eight default departments are seeded exactly once per database on first boot, tracked by a `default_departments_seeded` sentinel in `departments_meta`; after that the user owns the list and deleted departments stay deleted across restarts.\n\n### Company setup (onboarding)\nBoth registries are first populated by **conversational onboarding** at\n`/onboard`. The user describes their business in their own words (optionally\nattaching a deck or one-pager); the `onboarding_interviewer` agent — Council-\nvisible but outside `SPECIALIST_REGISTRY`, so the Executive cannot reach it via\n`consult_specialist` — asks up to 8 clarifying questions, then emits a draft\ncompany profile, a leadership roster, and a department list. The user reviews\nand edits that draft inline (the same section editors `/company-profile` uses)\nbefore anything is written.\n\nTwo properties matter here. First, **the interview never writes**: the whole\nexchange lives in an in-memory, TTL'd session, and the single write happens at\n`POST /onboard/interview/commit`, so a rejected save leaves the draft editable\nand retryable. Second, **the department step is additive** — drafted\ndepartments are matched onto existing ones by slug or title and updated in\nplace, unmatched ones are created, and nothing is ever deleted. That is\ndeliberately unlike the fixture loader's `_seed_departments`, which DELETEs\nevery row first: doing that here would drop the eight defaults along with their\n`specialist_key` wiring, which `create_department` (always `specialist_key =\nNULL`) cannot restore. `profile.org_structure` is derived from the reviewed\npeople and department tables at commit time, so it cannot drift from them.\n\nOnboarding marks exactly one person `is_principal` and gives them `WILDCARD`\nauthority; no contact details are imported, even if the user volunteers them, because\nchannels and handles are a deliberate action on the People page. The original\n12-step form wizard remains available at `/onboard?mode=form` and backs the\n`openexecutive onboard` CLI.\n\n### Departments\nEach department carries a charter, `Goal` rows, `ChecklistItem`s, and named cadences. Cadence specs (`daily@HH:MM`, `weekly@DOW@HH:MM`, `quarterly@DD-HH:MM`, UTC) enqueue a `department_check_in` workflow when due, then chain the next occurrence. A department may map to one of the 8 specialist agents via `specialist_key`, or stay informational. An assigned head gets a generated department-head voice persona.\n\nA department can also list **watched entities** (`watched_entities`, edited one per line on its page): vendors it is evaluating, competitors in its lane, tickers it tracks. The research watch policy treats them as strong grounding — a proposal about one can be added to the watch list on its own — and routes the resulting watch, its alerts and any suggestion cards to the department and its head rather than the principal (see External monitoring).\n\n### Authority gate\nEach department declares an `AuthorityLevel`: `propose_only` routes a scheduled action to an approver as a briefing alert (`propose_via_alert`) and marks the row done without dispatching — the pending-ness lives on the alert, not on a `wait_for_human` workflow gate. With no approver to route to, the row is failed instead (terminal, no retry) so the request stays visible. `auto_execute` (autonomous) fires directly. The approver is chosen by `AuthorityScope` — a scheduled action's `required_scope` (e.g. `vendor_onboarding`, `spend_lt_10k`) feeds `find_approvers(scope)`, which ranks non-principal scope-holders ahead of `WILDCARD` holders. A null/unknown scope falls back to `WILDCARD` (the principal).\n\n### People & channels\nThe people registry resolves each `Person` to a `(channel, channel_ref)` pair via `prefer_channel_for()`, honoring `preferred_channel` (falling back email → slack → discord → telegram) and availability windows. Departments can also set `slack_channel_id` / `discord_channel_id` / `telegram_chat_id` to broadcast to a team room instead of DMing the head.\n\n`is_principal` is **not patchable** — it is set at creation (the fixture loader or `POST /people`) and by an onboarding commit. `PersonPatch` carries no such field and the `upsert_person` chat tool refuses it, so a promotion cannot be talked into the Executive. Re-running onboarding is the one path that changes it on an existing row: it rewrites the flag on a name-matched person and demotes any principal left off the new roster. The flag is **not** single-occupancy: the index on `is_principal` is not UNIQUE, and `find_principal_person` resolves a multi-row case by lowest id, so co-principals are a supported state. That tie-break is **id order, not seniority**, so on a roster whose sitting principal has the higher id the fallback identity follows the lower-id row. `find_approvers` orders `is_principal ASC` and only returns people holding a scope row, so principal status changes approver *order*, never approver *membership*.\n\nA **zero-principal** roster is not a supported mode. The lookups return `None` cleanly but several consumers degrade rather than refuse — a `propose_only` action with no approver files its proposal as an unrouted alert and then fails, and the principal brief and artifact routing both drop silently. Archiving cannot get there: `archive_person` refuses the **last** active principal (`409` from `POST /people/{id}/archive`, an error from the chat tool), while a co-principal can still be archived as long as another remains.",
  "mermaid": "flowchart TD\n  People[\"People registry (Person, is_principal)\"]\n  Dept[\"Departments (8 seeded once)\"]\n  GC[\"Goals / checklists / cadences\"]\n  Head[\"Head + voice persona\"]\n  Gate[\"Authority gate (propose_only vs autonomous)\"]\n  Scope[\"AuthorityScope to find_approvers\"]\n  Chan[\"Channel resolution (Discord / Telegram / email)\"]\n  Dept --> GC\n  Dept --> Head\n  Dept -->|proposes action| Gate\n  Gate -->|required_scope| Scope\n  Scope -->|ranks approvers| People\n  People --> Chan\n  Dept -->|team room| Chan\n  class People storage\n  class Dept storage\n  class GC storage\n  class Gate compute\n  class Scope compute\n  class Chan compute\n  class Head compute",
  "generated_at": "2026-09-20T21:51:49.902575+00:00"
}
